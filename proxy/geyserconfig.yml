bedrock:
  address: 0.0.0.0
  port: 19132

java:
  address: localhost
  port: 25567
  auth-type: floodgate

motd:
  passthrough-player-counts: true

gameplay:
  server-name: "The COOP"

advanced:
  floodgate-key-file: /run/secrets/floodgate
  java:
    use-haproxy-protocol: true

debug-mode: true
